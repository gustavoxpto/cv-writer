# Copy to .env for local dev. Never commit .env itself (see .gitignore + docs/security.md).
# This file documents *which* variables are needed, with placeholder/dummy values only.

# EXAMPLE_API_KEY=

# Read by generation/rephraser.py's ClaudeRephraser at call time only — never written to disk,
# the database, or a generated artifact (spec criterion 23, docs/security.md). Leave unset here;
# export it in your real (gitignored) shell/.env for local use.
# ANTHROPIC_API_KEY=
