# Learning log

Build-to-learn journal. One entry per session (or whenever something clicks). Not for
changelog-style "what changed" — that's git/pairing-notes territory. This is for "what I now
understand that I didn't before."

```markdown
## 2026-08-17
- Set up the harness folder structure itself.
- Learned: why spec-driven dev puts a human sign-off *before* code, not just before merge — it
  catches wrong problems before wrong solutions get built.
- Still fuzzy: how encrypted-secrets-at-rest (sops/age) actually works end to end — haven't set
  it up yet, just documented the target in docs/security.md.
```
