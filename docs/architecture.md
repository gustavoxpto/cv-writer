# Architecture

Empty for now — this repo is a harness scaffold, not yet a specific application. Once `src/`
has real code, document the system shape here: major components, how they talk to each other,
and link out to the ADRs (`specs/adr/`) that explain *why* it's shaped that way.
