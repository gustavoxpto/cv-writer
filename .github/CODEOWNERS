# Require review from the repo owner on everything, until this grows real collaborators.
# Pairs with CLAUDE.md hard rule #3 (review before merge) and the branch-protection TODO in
# docs/security.md — CODEOWNERS only requests the review; branch protection is what requires it.
* @gustavoxpto
