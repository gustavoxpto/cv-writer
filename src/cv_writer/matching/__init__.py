"""Matching: turn a profile + posting requirement set into a deterministic match report.

See specs/features/001-cv-writer.md, section C (criteria 13-16). Pure functions, no I/O, no
LLM — this is "the deterministic, inspectable heart" the spec calls out.
"""
